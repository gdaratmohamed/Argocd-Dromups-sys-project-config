apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: default
spec:
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 5000 # Service port
    targetPort: 5000 # Pod port
  type: ClusterIP # Internal service